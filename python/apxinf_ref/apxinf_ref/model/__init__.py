"""Model implementations living behind an explicit ``--engine`` switch.

``pi05`` is the assembled eager Pi0.5 that this package maintains; ``vendor`` is
the untouched upstream snapshot kept as a fidelity anchor.
"""

from __future__ import annotations
